class HaloGeneric {
    sapaUser(user) {
      console.log(`Halo user ${user}`);
    }
  }
  
  // Pemanggilan fungsi
  const halo = new HaloGeneric();
  halo.sapaUser("SALMAN ALFARISI"); // Ganti dengan nama panggilan praktikan
  